_schema-version: 3.3.0
ID: Cash-Back-Project
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: Cash-Back-Project-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: Cash-Back-Project-auth
      - name: Cash-Back-Project-db
      - name: Cash-Back-Project-connectivity
      - name: Cash-Back-Project-destination

  - name: Cash-Back-Project-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: Cash-Back-Project-db

resources:
  - name: Cash-Back-Project-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: Cash-Back-Project-${org}-${space}
        tenant-mode: dedicated
  - name: Cash-Back-Project-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: Cash-Back-Project-connectivity
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite
  - name: Cash-Back-Project-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
